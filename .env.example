# Fallback: duplicate this file as .env only if the supplied blank .env is missing.
# Paste your own xAI inference key after the equals sign, then save and restart.
# Keep the real key out of chat, browser code, screenshots, and shared files.
XAI_API_KEY=
